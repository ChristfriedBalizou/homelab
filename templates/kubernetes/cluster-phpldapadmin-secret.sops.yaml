---
apiVersion: v1
kind: Secret
metadata:
  name: cluster-phpldapadmin-secrets
  namespace: identity
stringData:
  default.yaml: |
    PHPLDAPADMIN_LDAP_HOSTS:
     - ${SECRET_LDAP_DOMAIN}:
       - server:
         - host: ${SECRET_LDAP_SERVER}
         - port: ${SECRET_LDAP_SERVER_PORT}
         - tls: ${SECRET_LDAP_TLS}
       - login:
         - bind_id: ${SECRET_LDAP_ROOT}
         - attr: cn
