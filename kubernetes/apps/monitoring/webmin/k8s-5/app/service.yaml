apiVersion: v1
kind: Service
metadata:
  name: k8s-5
  namespace: monitoring
spec:
  ports:
  - name: k8s-5
    port: 10000
    protocol: TCP
    targetPort: 10000
  clusterIP: None
  type: ClusterIP
