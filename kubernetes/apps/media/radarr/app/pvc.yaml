---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-radarr
  namespace: media
  annotations:
    nfs.io/storage-path: "volume1/apps/radarr"
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: nfs
