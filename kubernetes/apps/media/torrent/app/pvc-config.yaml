---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: torrent-config
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
  storageClassName: nfs-client
