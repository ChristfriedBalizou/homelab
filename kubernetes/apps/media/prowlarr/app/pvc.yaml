---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-prowlarr
  namespace: media
  annotations:
    nfs.io/storage-path: "volume1/apps/prowlarr"
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: nfs
