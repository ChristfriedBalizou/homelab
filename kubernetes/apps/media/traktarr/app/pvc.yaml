---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: config-traktarr
spec:
  accessModes:
    - ReadWriteMany
  capacity:
    storage: 5Gi
  nfs:
    path: /mnt/nfs/volume1/apps/traktarr
    server: ${NFS_SERVER_IP}
  storageClassName: config-traktarr
  volumeMode: Filesystem
  persistentVolumeReclaimPolicy: Retain
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-traktarr
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: config-traktarr
  volumeMode: Filesystem
