---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-traktarr
  namespace: media
  annotations:
    nfs.io/storage-path: "volume1/apps/traktarr"
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: nfs
