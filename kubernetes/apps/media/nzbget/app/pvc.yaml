---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-nzbget
  namespace: media
  annotations:
    nfs.io/storage-path: "volume1/apps/nzbget"
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: nfs
