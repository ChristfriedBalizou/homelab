apiVersion: v1
kind: Service
metadata:
  name: hyperoptic
  namespace: networking
spec:
  ports:
  - name: hyperoptic
    port: 80
    protocol: TCP
    targetPort: 80
  clusterIP: None
  type: ClusterIP
