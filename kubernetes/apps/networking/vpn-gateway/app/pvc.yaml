---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pod-gateway-data
  namespace: networking
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
  storageClassName: local-path
