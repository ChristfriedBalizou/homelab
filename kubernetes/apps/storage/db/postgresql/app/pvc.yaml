---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgresql
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 150Gi
  nfs:
    path: /mnt/nfs/volume1/apps/db/postgresql
    server: ${NFS_SERVER_IP}
  storageClassName: postgresql
  volumeMode: Filesystem
  persistentVolumeReclaimPolicy: Retain
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgresql
  namespace: storage
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 150Gi
  storageClassName: postgresql
  volumeMode: Filesystem
