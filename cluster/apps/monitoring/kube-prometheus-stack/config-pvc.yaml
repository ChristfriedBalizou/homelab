---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prometheus-config
  namespace: monitoring
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 500Mi
