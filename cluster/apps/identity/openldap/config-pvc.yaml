---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openldap-filesystem
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 512Mi
