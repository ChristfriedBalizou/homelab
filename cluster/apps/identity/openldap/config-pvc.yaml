---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openldap-filesystem
  namespace: identity
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 512Mi
