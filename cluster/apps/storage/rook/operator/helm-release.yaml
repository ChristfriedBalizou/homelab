---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: rook-ceph
  namespace: storage
spec:
  interval: 10m
  chart:
    spec:
      chart: rook-ceph
      version: v1.10.6
      sourceRef:
        kind: HelmRepository
        name: rook-charts
        namespace: flux-system
  values:
    monitoring:
      enabled: true
